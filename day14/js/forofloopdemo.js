/*
 for...of  it used to iterate over value of an iterable

 arrays
 string
 set
 maps

 for (let variable of iterable){
 }
*/

let arr = ["aayush", "sriyash", "vamika"];
for (let name of arr) {
  console.log("name is", name);
}

let msg = "good evening class";
for (let ch of msg) {
  console.log(ch);
}
